# React-Native-Todo-Task-Tracker
A high-performance and scalable Todo application built using React Native for the frontend, Convex as the backend and AsyncStorage (for theme preference). This app focuses on delivering a smooth user experience with optimized list rendering and efficient state management.
